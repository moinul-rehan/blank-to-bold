export function Footer() {
  return (
    <footer>
      <div className="mx-auto max-w-[var(--layout-max-width)] px-[var(--layout-gutter)]">
        {/* TODO: footer content — awaits brand/experience direction */}
      </div>
    </footer>
  );
}
