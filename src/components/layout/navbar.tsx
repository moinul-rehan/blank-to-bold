export function Navbar() {
  return (
    <header>
      <nav
        aria-label="Primary"
        className="mx-auto flex h-[var(--layout-header-height)] max-w-[var(--layout-max-width)] items-center px-[var(--layout-gutter)]"
      >
        {/* TODO: nav content — awaits brand/experience direction */}
      </nav>
    </header>
  );
}
